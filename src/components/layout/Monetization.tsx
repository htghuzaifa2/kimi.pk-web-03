'use client';

// Ad placeholder component — keeps the import in layout.tsx intact.
// Previously hosted a smart-link timer (Adsterra). Now a no-op shell
// so the placement is preserved for future ad partners.

export default function Monetization() {
  return null;
}
